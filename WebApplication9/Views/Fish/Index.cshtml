@model IEnumerable<WebApplication9.Models.Fish>


    <style>
            /*.carousel-inner > .item {
            height: 200px;
        }*/
            .carousel img {
            border-radius: 50px;
            padding: 10px;
            cursor: pointer;
            transition: 5.3s;
            float: left;
            width: 200px;
            height: 200px;
            max-width: 200px;
            max-height: 200px;
            animation-name: zoom;
            animation-duration: 1.6s;
            opacity: 1;
        }

            .myImg:hover {
                -webkit-filter: brightness(30%); /* Safari */
                filter: brightness(125%);
                opacity: 1;
            }
                /* width: inherit;
            object-fit: contain;*/
            }
           /*.carousel-inner > item > img {
            width: 150px;
            height: 20px;
        }*/
    </style>
<br />

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@{ var interval = 2000;
foreach(var item in Model) {
    if(item.Name != null) {
            interval += 2345;

        <div class="col-lg-2 col-md-4 col-sm-6 col-xs-12" style="float : left">
            <h4>        @Html.DisplayFor(modelItem => item.Name)    </h4>
            <hr>
            <h5>        @Html.DisplayFor(modelItem => item.ScientificName)    </h5>

            @*<img src="@Url.Action("CoralPhoto", "Corals", new { id = item.CoralId })" width="150" />*@
            <div id="myCarousel" class="carousel slide" data-ride="carousel" data-interval="@interval" onclick="f1(@item.FishId, this)">
                <!-- Indicators -->
                @*<ol class="carousel-indicators">
                    <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
                    <li data-target="#myCarousel" data-slide-to="1"></li>
                    <li data-target="#myCarousel" data-slide-to="2"></li>
                </ol>*@

                <!-- Wrapper for slides -->
                <div class="carousel-inner">
                    <div class="item active">
                        <img src="@Url.Action("FishPhoto", "Fish", new { id = item.FishId, number = 0 })" />
                    </div>

                    <div class="item">

                        <img src="@Url.Action("FishPhoto", "Fish", new { id = item.FishId, number = 1 })" />
                    </div>

                    <div class="item">

                        <img src="@Url.Action("FishPhoto", "Fish", new { id = item.FishId, number = 2 })" />
                    </div>
                </div>

                <!-- Left and right controls -->
                @*<a class="left carousel-control" href="#myCarousel" data-slide="prev">
                        <span class="glyphicon glyphicon-chevron-left"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="right carousel-control" href="#myCarousel" data-slide="next">
                        <span class="glyphicon glyphicon-chevron-right"></span>
                        <span class="sr-only">Next</span>
                    </a>*@
            </div>

            @*<p>
                <a class="btn btn-primary" data-toggle="collapse" href="#multiCollapseExample1"
                   role="button" aria-expanded="false" aria-controls="multiCollapseExample1">Details</a>
            </p>


            <div class="collapse multi-collapse" id="multiCollapseExample1">

                @Html.DisplayFor(modelItem => item.Details)
            </div>*@




            @*<p>            @Html.DisplayFor(modelItem => item.Details)                       </p>*@

            @if(User.Identity.Name == item.UploadedBy) {
                <div>
                    @Html.ActionLink("Edit", "Edit", new { id = item.FishId })
                </div>

                @Html.ActionLink("Delete", "Delete", new { id = item.FishId })

    }



        </div>
        }
    }
        }
<div></div>
 
<script>
  

    function f1(item) {

        window.location = "/Fish/View/" + item;

        @*var url = '@Url.Action("CoralPhoto", "Corals")';
        url += '?id=' + item;
        $("#attachments").load(url);*@


    }
</script>

