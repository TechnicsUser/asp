@model WebApplication9.Models.RegisterViewModel
@{
    ViewBag.Title = "Register";
}

<h2>@ViewBag.Title.</h2>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>

<script>
    function readURL(input) {
        alert("hi");
        if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
                //$('.blah').attr('src', e.target.result);
                var output = document.getElementById('blah');
                output.src = reader.result;
            };

            reader.readAsDataURL(input.files[0]);
        }
    }

   

    window.onload = function () {

        var x1 = document.getElementById("reg");
        x1.addEventListener("click", getGeo, false);

    };
    function getGeo() {
        var x = document.getElementById("geo");
        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(showPosition);

        } else {
            x.innerHTML = "Geolocation is not supported by this browser.";
        }
    }
    function showPosition(position) {
        document.getElementById("latitude").value = position.coords.latitude;
        document.getElementById("longitude").value = position.coords.longitude;
        //var x = document.getElementById("geo");

        //x.textContent = "Latitude: " + position.coords.latitude;

    }
</script>

@using(Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form", enctype = "multipart/form-data" })) {
    @Html.AntiForgeryToken()
    <h4>Create a new account.</h4>
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })




    <div class="form-group">
        @*@Html.LabelFor(m => m.LocationLat, new { @class = "col-md-2 control-label" })*@
        <div class="col-md-10">
            @Html.HiddenFor(m => m.LocationLat, new { @class = "form-control", @id = "latitude" })

            @*@Html.DisplayFor(m => m.LocationLon, new { @Value = "new test" })*@
            @*<input type="hidden" id="Longitude" name="Longitude" value="@Model.LocationLon" />
                <input type="hidden" id="Latitude" name="Latitude" value="@Model.LocationLat" />*@
            @*@Html.HiddenFor(m => m.LocationLon)
                @Html.HiddenFor(m => m.LocationLat)*@

        </div>
    </div>
    <div class="form-group">
        @*@Html.LabelFor(m => m.LocationLon, new { @class = "col-md-2 control-label" })*@
        <div class="col-md-10">
            @*@Html.HiddenFor(m => m.LocationLon, new { @class = "form-control", @id = "longitude" })*@


        </div>
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.LocationLon, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.LocationLon, new { @class = "form-control" })
        </div>
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.LocationLat, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.LocationLat, new { @class = "form-control" })
        </div>
    </div>



    <div class="form-group">
        @*@Html.LabelFor(m => m.FirstName, new { @class = "col-md-2 control-label" })*@
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control", @placeholder = "First name" })
        </div>
    </div>

    <div class="form-group">
        @*@Html.LabelFor(m => m.LastName, new { @class = "col-md-2 control-label" })*@
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.LastName, new { @class = "form-control" , @placeholder = "Last name" })
        </div>
    </div>

    <div class="form-group">
        @*@Html.LabelFor(m => m.IdUserName, new { @class = "col-md-2 control-label" })*@
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.IdUserName, new { @class = "form-control", @placeholder = "User name" } )
        </div>
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.UserPhoto, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">

            <input type="file" name="UserPhoto" class="imgInp" id="fileUpload" accept=".png,.jpg,.jpeg,.gif,.tif" onchange="readURL(this)" />

        </div>
    </div>
    <img id="blah" src="#" alt="" width="100"/>

    <div class="form-group">
        @*@Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })*@
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control", @placeholder = "E-mail" })
        </div>
    </div>

    <div class="form-group">
        @*@Html.LabelFor(m => m.Password, new { @class = "col-md-2 control-label" })*@
        <div class="col-md-10">
            @Html.PasswordFor(m => m.Password, new { @class = "form-control", @placeholder = "Password" })
        </div>
    </div>
    <div class="form-group">
        @*@Html.LabelFor(m => m.ConfirmPassword, new { @class = "col-md-2 control-label" })*@
        <div class="col-md-10">
            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control", @placeholder = "Confirm password" })
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="button" class="btn btn-default" value="Put me on the map !!!" id="reg" />
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Register" />
        </div>
    </div>
    }

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

@*IdUserName = c.String(nullable: false, maxLength: 128),
    FirstName = c.String(nullable: false, maxLength: 128),
    LastName = c.String(nullable: false, maxLength: 128),
    LocationLat = c.String(nullable: false, maxLength: 128),
    LocationLon = c.String(nullable: false, maxLength: 128),
    UserPhoto = c.Binary(nullable: false, maxLength: 128),*@
